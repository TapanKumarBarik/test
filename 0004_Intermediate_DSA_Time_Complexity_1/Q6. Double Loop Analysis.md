What is the time complexity of the following code :
```python
for i in range(n):
    for j in range(i + 1):
        print(i + j, end=' ')
    print()
```
# # # # ANSWER
O(n^2)
# # # # EXPLANATION
The outer loop runs `n` times, and the inner loop runs `i + 1` times for each iteration of the outer loop. The total number of iterations of the inner loop can be calculated as follows: